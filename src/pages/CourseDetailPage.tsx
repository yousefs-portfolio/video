import React from 'react';
import { Box, Typography } from '@mui/material';

const CourseDetailPage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">CourseDetailPage</Typography>
    </Box>
  );
};

export default CourseDetailPage;
