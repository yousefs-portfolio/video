import React from 'react';
import { Box, Typography } from '@mui/material';

const VideoPlayerPage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">VideoPlayerPage</Typography>
    </Box>
  );
};

export default VideoPlayerPage;
